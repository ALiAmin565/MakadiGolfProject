<div>
    <label for="{{ $id }}">{!! $title !!} </label>
    <input type="checkbox" id="{{ $id }}" name="{{ $id }}" value="{{ $value }}"
        class="checkbox-design">
    <span id="{{ $priceId }}" class="spanCheckBox"></span>
</div>